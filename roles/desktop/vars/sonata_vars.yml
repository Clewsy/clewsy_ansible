---
sonata_configs:
  - { name: 'names[0]', value: "{{ host_name }}" }
  - { name: 'ports[0]', value: "{{ mpd.port | default('6600') }}" }
  - { name: 'musicdirs[0]', value: "{{ mpd.music_dir | default(ansible_user_home_dir + '/music') }}" }
