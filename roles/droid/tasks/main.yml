---
- name: '[termux_ssh] Add keys and configure ssh.'
  include_tasks: termux_ssh.yml

- name: '[termux_packages] Install packages.'
  include_tasks: termux_packages.yml

- name: '[termux_scripts] Install and customize scripts for use with termux.'
  include_tasks: termux_scripts.yml

- name: '[termux_misc] Termux miscellaneous configuration.'
  include_tasks: termux_misc.yml

- name: '[termux_shortcuts] Configure shortcuts for use with the Termux widget.'
  include_tasks: termux_shortcuts.yml
