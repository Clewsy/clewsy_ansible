---
- name: '[termux_ssh] Add keys and configure ssh.'
  include: termux_ssh.yml

- name: '[termux_packages] Install packages.'
  include: termux_packages.yml

- name: '[termux_scripts] Termux miscellaneous configutation.'
  include: termux_scripts.yml

- name: '[termux_misc] Termux miscellaneous configutation.'
  include: termux_misc.yml

- name: '[termux_shortcuts] Termux miscellaneous configutation.'
  include: termux_shortcuts.yml
