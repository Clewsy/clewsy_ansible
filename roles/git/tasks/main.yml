---
- name: Install git.
  apt:
    name: git
    state: latest

- name: Set git user defaults (.gitconfig).
  copy:
    src: files/.gitconfig
    dest: "/home/{{ ansible_user }}/.gitconfig"
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: 0664