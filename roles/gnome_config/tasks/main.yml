---
- name: Copy desktop profile image to {{ profile_icon_dest }} (expected location for distribution {{ ansible_distribution }}).
  copy:
    src: "files/{{ ansible_user }}"
    dest: "{{ profile_icon_dest }}"
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: 0644
