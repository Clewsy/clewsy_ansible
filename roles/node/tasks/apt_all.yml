---
- name: '[apt_all] Copy apt_all.sh script to user bin directory.'
  get_url:
    url: "{{ apt_all_script_url }}"
    dest: "{{ ansible_user_dir }}/bin/apt_all.sh"
    force: yes
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: '0755'
