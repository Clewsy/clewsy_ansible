---
- name: '[whodis] Copy whodis.sh script to user bin directory.'
  get_url:
    url: "{{ whodis_script_url }}"
    dest: "/home/{{ ansible_user }}/bin/whodis.sh"
    owner: "{{ ansible_user }}"
    group: "{{ ansible_user }}"
    mode: '0755'
    